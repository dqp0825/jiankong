"""jiankong URL Configuration

The `urlpatterns` list routes URLs to views. For more information please see:
    https://docs.djangoproject.com/en/1.11/topics/http/urls/
Examples:
Function views
    1. Add an import:  from my_app import views
    2. Add a URL to urlpatterns:  url(r'^$', views.home, name='home')
Class-based views
    1. Add an import:  from other_app.views import Home
    2. Add a URL to urlpatterns:  url(r'^$', Home.as_view(), name='home')
Including another URLconf
    1. Import the include() function: from django.conf.urls import url, include
    2. Add a URL to urlpatterns:  url(r'^blog/', include('blog.urls'))
"""
from django.conf.urls import url,include
from django.contrib import admin


urlpatterns = [
    url(r'^admin/', admin.site.urls),
    url(r'^alarmEvents/',include('alarmEvents.urls',namespace = 'alarmEvents')),
    url(r'^dashBoard/',include('dashBoard.urls', namespace = 'dashBoard')),
    url(r'^realtimeMonitor/',include('realtimeMonitor.urls', namespace = 'realtimeMonitor')),
    url(r'^signbackInformation/',include('signbackInformation.urls', namespace = 'signbackInformation')),
    url(r'^autoDiscovery/',include('autoDiscovery.urls', namespace = 'autoDiscovery')),
    url(r'^monitorItems/',include('monitorItems.urls', namespace = 'monitorItems')),
    url(r'^reportTable/',include('reportTable.urls', namespace = 'reportTable')),
    url(r'^topologyView/',include('topologyView.urls', namespace = 'topologyView')),
]

